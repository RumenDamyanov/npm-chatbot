/**
 * Minimal Chatbot test to check basic functionality
 */

describe('Chatbot Minimal Test', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true);
  });

  it('should handle numbers', () => {
    expect(1 + 1).toBe(2);
  });
});